# drop-in: /etc/systemd/journald.conf.d/99-fiesta.conf
# Bounds the systemd journal so it stays within the 10 GB total-logs budget
# (2 GB journal + 8 GB file logs). Apply with: systemctl restart systemd-journald
[Journal]
SystemMaxUse=2G
MaxRetentionSec=1month
